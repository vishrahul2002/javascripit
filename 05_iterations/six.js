const coding = ['java', 'python', 'c++', 'c#', 'javascript'];

const values = coding.forEach ( (item) => {
    // console.log(item);
    return item
})

console.log(values); // for each does not return the value for that we can use map or somthing else